.DS_Store
node_modules/
*.log

# Personal trade data — keep local only, sync via Export/Import buttons
assets/seed_trades.json
.claude/settings.local.json

# Desktop launcher — local path, not for the repo
JayBot.html
JayBot.command
